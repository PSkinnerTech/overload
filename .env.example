# Motion API Configuration
# You can use either API Key authentication OR OAuth2 (not both)

# Option 1: API Key Authentication (Simpler)
# Get your API key from https://app.usemotion.com/settings/api
MOTION_API_KEY=your-motion-api-key

# Option 2: OAuth2 Authentication (More complex, but doesn't expose API key)
# MOTION_CLIENT_ID=your-motion-client-id
# MOTION_CLIENT_SECRET=your-motion-client-secret

# Optional: Override default sync intervals (in milliseconds)
# MOTION_SYNC_INTERVAL_TASKS=300000
# MOTION_SYNC_INTERVAL_PROJECTS=3600000